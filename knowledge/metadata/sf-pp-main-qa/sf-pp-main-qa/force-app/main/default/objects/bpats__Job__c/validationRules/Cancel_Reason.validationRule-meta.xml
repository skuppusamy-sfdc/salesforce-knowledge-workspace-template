<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Cancel_Reason</fullName>
    <active>true</active>
    <description>If a job status is set to canceled, then I a cancel will be required.</description>
    <errorConditionFormula>AND (ISPICKVAL(bpats__Status__c,&quot;Canceled&quot;),
ISBLANK(Text( Cancel_Reason__c )))</errorConditionFormula>
    <errorDisplayField>Cancel_Reason__c</errorDisplayField>
    <errorMessage>If a job status is set to canceled, a cancel reason is required.</errorMessage>
</ValidationRule>
