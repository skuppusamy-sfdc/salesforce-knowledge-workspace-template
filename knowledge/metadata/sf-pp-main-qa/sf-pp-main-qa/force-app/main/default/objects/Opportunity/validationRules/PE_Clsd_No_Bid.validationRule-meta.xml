<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>PE_Clsd_No_Bid</fullName>
    <active>false</active>
    <description>Verifies required fields are populated when Stage is Closed No-Bid for PE services.</description>
    <errorConditionFormula>RecordType.Name = &quot;Patient Engagement Opportunity&quot; &amp;&amp;

ISPICKVAL(StageName, &quot;Closed-No Bid&quot;)&amp;&amp;
  
(ISNULL(CloseDate)||
 ISNULL(Lost_Date__c))</errorConditionFormula>
    <errorMessage>Required Fields: Lost Date and Close Date.</errorMessage>
</ValidationRule>
